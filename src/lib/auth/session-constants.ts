/** HTTP-only session cookie name (PRD: quiz master). */
export const SESSION_COOKIE_NAME = "qm_session";

/** JWT / cookie lifetime (keep in sync). */
export const SESSION_TOKEN_EXPIRES = "7d";

export const SESSION_MAX_AGE_SECONDS = 7 * 24 * 60 * 60;
